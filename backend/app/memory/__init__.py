"""Memory subsystem.

Short-term (conversation), episodic (every interaction), semantic
(confirmed facts), and procedural (approved workflows) memory.
Domain-isolated by design — see docs/SECURITY.md.
"""
